info:
  name: Image upload
  type: http
  seq: 3

http:
  method: POST
  url: POST http://localhost:5000/api/jobs/2/assets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
