info:
  name: GET THE CONTEXT
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/jobs/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
